"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const runtime_1 = require("@prisma/client/runtime");
const logger_1 = __importDefault(require("../../utils/logger"));
/**
 * Middleware that handles errors generated by the prisma orm.
 * @param err Error
 * @param _req Request
 * @param res Response
 * @param next NextFunction
 * @returns
 */
function prismaErrorHandler(err, _req, res, next) {
    var _a, _b;
    return __awaiter(this, void 0, void 0, function* () {
        if (!(err instanceof runtime_1.PrismaClientKnownRequestError))
            return next(err);
        logger_1.default.debug(`treating PrismaKnowRequestError with code ${err.code}`);
        switch (err.code) {
            case "P2002":
                return res
                    .status(422)
                    .json({ errors: [`the field ${(_a = err.meta) === null || _a === void 0 ? void 0 : _a.target} is not unique`] });
            case "P2025":
                return res.status(422).json({
                    errors: [`${(_b = err.meta) === null || _b === void 0 ? void 0 : _b.cause}`],
                });
            default:
                logger_1.default.debug(`Unhandled error with code ${err.code} in prismaErrorHandler`);
                return res.sendStatus(500);
        }
    });
}
exports.default = prismaErrorHandler;
